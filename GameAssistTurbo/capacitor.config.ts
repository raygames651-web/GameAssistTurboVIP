import { CapacitorConfig } from '@capacitor/cli'; 
const config: CapacitorConfig = { 
  appId: 'com.gameassist.turbo', 
  appName: 'Game Assist Turbo', 
  webDir: 'dist', 
  server: { androidScheme: 'https' } 
}; 
export default config; 
